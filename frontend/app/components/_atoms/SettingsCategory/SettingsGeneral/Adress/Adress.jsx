import React from "react";
import styles from "./Adress.module.scss";

const Adress = () => {
    return <input type="text" placeholder="Adress" className={styles.adress_input} />;
};

export default Adress;